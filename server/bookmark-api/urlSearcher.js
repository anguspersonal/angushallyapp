/* For a given url, search the web for metadata.

Take a url as an input, and return a list of metadata.

Steps:
1. Take a url as an input.
2. Search the web for metadata.
3. Return the metadata.

*/

const urlSearcher = async (url) => {